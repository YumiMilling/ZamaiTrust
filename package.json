{
  "name": "catsp-os",
  "version": "0.5.0",
  "private": true,
  "description": "CATSP OS — Modular platform for agricultural transformation",
  "scripts": {
    "dev:site": "pnpm --filter @catsp/site dev",
    "build:site": "pnpm --filter @catsp/site build",
    "dev:web": "pnpm --filter @catsp/web dev",
    "build:web": "pnpm --filter @catsp/web build",
    "db:migrate": "supabase db push",
    "db:reset": "supabase db reset",
    "db:seed": "supabase db seed"
  }
}
